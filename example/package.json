{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --experimental-modules",
    "start:prod": "cross-env NODE_ENV=prod node index.js --experimental-modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv-ex": "file:.."
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
